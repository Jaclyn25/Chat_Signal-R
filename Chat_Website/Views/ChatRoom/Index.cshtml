@using Chat_Website.Models.DataBase
@model IEnumerable<UserApplication>

@{
    ViewData["Title"] = "Chats";
}

<style>
    .contacts-container {
        background: #F8F9FA;
        min-height: 100vh;
        padding: 0;
    }

    .contacts-header {
        background: #FFFFFF;
        color: #2C3E50;
        padding: 24px;
        position: sticky;
        top: 60px;
        z-index: 100;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
        border-bottom: 1px solid #E8E8E8;
    }

    .contacts-header h2 {
        margin: 0;
        font-weight: 700;
        font-size: 24px;
        color: #2C3E50;
    }

    .search-container {
        padding: 16px 20px;
        background: #F8F9FA;
        border-bottom: 1px solid #E8E8E8;
    }

    .search-input {
        background: #FFFFFF;
        border: 1px solid #E8E8E8;
        color: #2C3E50;
        border-radius: 24px;
        padding: 10px 16px;
        font-size: 14px;
    }

    .search-input::placeholder {
        color: #95A5A6;
    }

    .search-input:focus {
        outline: none;
        background: #FFFFFF;
        border-color: #B0B0B0;
        box-shadow: 0 0 0 3px rgba(208, 208, 208, 0.1);
    }

    .contacts-list {
        background: #F8F9FA;
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .contact-item {
        display: flex;
        align-items: center;
        padding: 16px 20px;
        border-bottom: 1px solid #E8E8E8;
        cursor: pointer;
        transition: background 0.2s ease;
        text-decoration: none;
        color: #2C3E50;
    }

    .contact-item:hover {
        background: #F0F0F0;
    }

    .contact-avatar {
        width: 56px;
        height: 56px;
        border-radius: 50%;
        background: linear-gradient(135deg, #E0E0E0 0%, #D0D0D0 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #2C3E50;
        font-weight: bold;
        font-size: 20px;
        margin-right: 16px;
        flex-shrink: 0;
        object-fit: cover;
    }

    .contact-avatar img {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        object-fit: cover;
    }

    .contact-info {
        flex: 1;
        min-width: 0;
    }

    .contact-name {
        font-weight: 600;
        font-size: 15px;
        margin-bottom: 6px;
        color: #2C3E50;
    }

    .contact-phone {
        font-size: 13px;
        color: #95A5A6;
        display: flex;
        align-items: center;
    }

    .contact-phone i {
        margin-right: 6px;
        font-size: 12px;
    }

    .contact-action {
        width: 44px;
        height: 44px;
        border-radius: 50%;
        background: #D0D0D0;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #2C3E50;
        transition: all 0.2s ease;
        margin-left: 12px;
        flex-shrink: 0;
        border: none;
        text-decoration: none;
    }

    .contact-action:hover {
        background: #B0B0B0;
        transform: scale(1.05);
        color: #2C3E50;
    }

    .contact-action i {
        font-size: 18px;
    }

    .empty-state {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 60px 20px;
        color: #94a3b8;
        min-height: 400px;
    }

    .empty-state i {
        font-size: 64px;
        margin-bottom: 16px;
        color: #475569;
    }

    .empty-state p {
        font-size: 16px;
        margin: 0;
    }
</style>

<div class="contacts-container">
    <!-- Header -->
    <div class="contacts-header">
        <h2 style="font-family: 'Poppins', sans-serif; font-weight: 700; letter-spacing: -0.5px; color: #1a1a1a;"><i class="bi bi-chat-dots-fill me-2" style="color: #1a1a1a;"></i>Messages</h2>
    </div>

    <!-- Search Bar -->
    <div class="search-container">
        <input type="text" class="search-input w-100" placeholder="Search people..." id="userSearch">
    </div>

    <!-- Contacts List or Empty State -->
    @if (!Model.Any())
    {
        <div class="empty-state">
            <i class="bi bi-inbox"></i>
            <p>No people available</p>
        </div>
    }
    else
    {
        <ul class="contacts-list" id="contactsList">
            @foreach (var user in Model)
            {
                var userInitial = user.UserName?.FirstOrDefault().ToString().ToUpper() ?? "?";
                <li class="contact-item" data-name="@user.UserName?.ToLower() ?? ''">
                    <!-- Avatar -->
                    <div class="contact-avatar" style="color: #1a1a1a; font-weight: 700;">
                        @if (!string.IsNullOrEmpty(user.ProfileImageUrl))
                        {
                            <img src="@user.ProfileImageUrl" alt="@user.UserName" />
                        }
                        else
                        {
                            @userInitial
                        }
                    </div>

                    <!-- Contact Info -->
                    <div class="contact-info">
                        <div class="contact-name" style="font-family: 'Poppins', sans-serif; font-weight: 700; letter-spacing: 0.2px;">@user.UserName</div>
                        @if (!string.IsNullOrEmpty(user.PhoneNumber))
                        {
                            <div class="contact-phone" style="font-weight: 500; font-family: 'Segoe UI', sans-serif;">
                                <i class="bi bi-telephone-fill"></i>
                                @user.PhoneNumber
                            </div>
                        }
                    </div>

                    <!-- Message Button -->
                    <a asp-action="PrivateChat" asp-controller="ChatRoom" asp-route-receiverId="@user.Id" 
                       class="contact-action" title="Send Message" style="color: #1a1a1a; font-weight: 700;">
                        <i class="bi bi-chat-dots-fill"></i>
                    </a>
                </li>
            }
        </ul>
    }
</div>

<script>
    // Search functionality
    document.getElementById('userSearch')?.addEventListener('input', function(e) {
        const searchTerm = e.target.value.toLowerCase();
        const items = document.querySelectorAll('.contact-item');
        
        items.forEach(item => {
            const name = item.getAttribute('data-name');
            if (name.includes(searchTerm)) {
                item.style.display = 'flex';
            } else {
                item.style.display = 'none';
            }
        });
    });
</script>